---
import Button from '../components/Button.astro';
const { imageURL, title } = Astro.props
---
<article>
	<img src={imageURL} alt={title} />
	<h2>{title}</h2>
    <Button href={imageURL} text={title} />
</article>